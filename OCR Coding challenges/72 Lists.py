print("*** Lists ***")

initialL = list(range(int(input("Range: "))))

withAdded = initialL.insert(int(input("Index to add the number: ")), int(input("Number to add: ")))

# I don't understand what the next step means
